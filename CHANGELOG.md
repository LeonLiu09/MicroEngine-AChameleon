# Changelog

SkillSwap 的稳定历史基线使用 annotated Git 标签保存，功能合并版本也可通过对应分支访问。更早的概念文档和提交仍完整保留在 Git 历史中。

## 未发布

- 新增 Python 标准库后端和自动初始化的 SQLite 数据库。
- 将 v4.2 邮箱登录表单接入真实认证接口，使用 PBKDF2 密码哈希和 HttpOnly 会话 Cookie。
- 保留创建账号、Daniel 免登录演示及其余前端功能的原有行为。

## v4.2 - 2026-08-18

- 重做 Settings 首页，将资料、技能、时间、通知、隐私、收藏与支持整理为清晰的分组入口。
- 将可约时间与地点改为按周日历和时间表，可新增连续时段、点击日程格并高亮已选时间。
- 统一设置子页交互：仅“保存修改”会写入数据，返回设置时放弃未保存的草稿，不再显示“取消”按钮。
- 将头像文件选择改为与设置界面一致的卡片式控件，并保留格式、大小、预览与保存校验。
- 新增独立 `v4.2.html`，保留 `index.html` 历史版本，并统一页面与文档的当前版本标识。

## v4.0 - 2026-08-17

- 合并“气泡胶囊优化版”和“聊天+技能认证v1.0”，保留两个源版本的提交历史。
- 保留 v3 Search 与 Matches 全部交互，并把桌面胶囊导航和移动底栏扩展为五项。
- 增加已连接联系人聊天、消息持久化、表情输入、模拟回复和媒体操作占位反馈。
- 增加覆盖八类技能的十题自适应测评、客观评级、认证徽章和个人资料入口。
- 保持 `STATE_VERSION = 1`，旧版本地数据会自动补齐聊天和认证字段。
- 本版本发布在 `v4.0` 分支，不修改 `main`，不创建版本标签。

## v3.0 - 2026-08-17

- 将 Search 的关键词输入与筛选区分离，替换为支持搜索、键盘和焦点恢复的自定义下拉。
- 保留国家与城市联动、单选语言、即时筛选和提交式关键词搜索。
- 将 Matches 重构为带数量和 URL 状态的三标签页。
- 增加联系人收藏、发起交换、取消收藏和确认取消待回应请求。
- 更新中英文版本文案、响应式布局和内置回归测试。

## v2.0 - 2026-08-17

- 完成 SkillSwap 双语单文件前端、Discover、Search、Matches、Settings 和本地持久化。
- 增加国家城市筛选、完整伙伴资料、本地头像处理及 GitHub Pages 发布。
- 标签指向 v3.0 开始修改前的完整远端基线，并包含当时已有的项目说明文档。
