# Changelog

SkillSwap 的稳定历史基线使用 annotated Git 标签保存，功能合并版本也可通过对应分支访问。更早的概念文档和提交仍完整保留在 Git 历史中。

## v5.3 - 2026-08-19

- 全站采用 HarmonyOS Sans SC 字体，统一标题字重、字号、行高与品牌标识。
- 未登录首页、登录页和注册页从中英双语标题词库中随机展示宣传语。
- 将正式前端从历史文件名 `v4.2.html` 重命名为唯一入口 `index.html`，移除旧跳转页和旧文件名路由。
- 更新 README，使 `main` 展示当前 v5.3 的功能、运行方式、入口和部署信息。

## v5.0 - 2026-08-18

- 合并 `main` 与 `后端搭建` 的完整历史，以真实 Python/SQLite 后端版本作为 v5.0 产品基线。
- 在已接受或已完成的交换请求卡片上增加“开始聊天”入口，并区分重连、后端版本不匹配和登录失效状态。
- 交换请求和状态每 5 秒静默自动同步，并在标签页重新可见或获得焦点时立即刷新，无需手动刷新页面。

- 新增 Python 标准库后端和自动初始化的 SQLite 数据库。
- 将 v4.2 邮箱登录表单接入真实认证接口，使用 PBKDF2 密码哈希和 HttpOnly 会话 Cookie。
- 保留创建账号和 Daniel 普通演示账号，并将资料、技能和交换流程接入后端。
- 新增仅限本机访问的中文超级管理员后台，支持账号 CRUD、密码重置、会话撤销、筛选分页和审计日志。
- 增加幂等数据库迁移与迁移前自动备份，并隔离普通用户和管理员会话。
- 管理员接口隐藏认证敏感字段，并增加同源、CSRF、自我/最后管理员保护和登录限流。
- 新增无第三方依赖的 `.env` 本地配置读取与可选管理员同步，真实配置文件默认不进入 Git。
- 合并用户资料、双数据库技能目录、真实用户搜索、社区统计和交换请求完整生命周期。
- 将技能目录管理统一迁入独立管理员后台，并为技能写操作增加 CSRF 校验和审计记录。
- 旧 `is_admin` 数据库会在备份后迁移为 `user/superadmin` 角色，现有资料、技能和交换请求保持不变。
- 数据库升级到 v4，迁移前备份现有账号库，并新增保留 30 天的真实文字消息表，不改写账号、资料、技能、会话或交换请求。
- 用 SQLite 与 HTTP 长轮询替换聊天随机词库回复；仅已接受或已完成交换的双方可聊天，并支持历史分页、幂等发送、自动重连和双标签页独立账号。
- 视频、拍照与图片发送仍为开发中占位，不上传或保存媒体；表情按普通文字发送。

## v4.2 - 2026-08-18

- 重做 Settings 首页，将资料、技能、时间、通知、隐私、收藏与支持整理为清晰的分组入口。
- 将可约时间与地点改为按周日历和时间表，可新增连续时段、点击日程格并高亮已选时间。
- 统一设置子页交互：仅“保存修改”会写入数据，返回设置时放弃未保存的草稿，不再显示“取消”按钮。
- 将头像文件选择改为与设置界面一致的卡片式控件，并保留格式、大小、预览与保存校验。
- 新增独立 `v4.2.html`，保留 `index.html` 历史版本，并统一页面与文档的当前版本标识。

## v4.0 - 2026-08-17

- 合并“气泡胶囊优化版”和“聊天+技能认证v1.0”，保留两个源版本的提交历史。
- 保留 v3 Search 与 Matches 全部交互，并把桌面胶囊导航和移动底栏扩展为五项。
- 增加已连接联系人聊天、消息持久化、表情输入、模拟回复和媒体操作占位反馈。
- 增加覆盖八类技能的十题自适应测评、客观评级、认证徽章和个人资料入口。
- 保持 `STATE_VERSION = 1`，旧版本地数据会自动补齐聊天和认证字段。
- 本版本发布在 `v4.0` 分支，不修改 `main`，不创建版本标签。

## v3.0 - 2026-08-17

- 将 Search 的关键词输入与筛选区分离，替换为支持搜索、键盘和焦点恢复的自定义下拉。
- 保留国家与城市联动、单选语言、即时筛选和提交式关键词搜索。
- 将 Matches 重构为带数量和 URL 状态的三标签页。
- 增加联系人收藏、发起交换、取消收藏和确认取消待回应请求。
- 更新中英文版本文案、响应式布局和内置回归测试。

## v2.0 - 2026-08-17

- 完成 SkillSwap 双语单文件前端、Discover、Search、Matches、Settings 和本地持久化。
- 增加国家城市筛选、完整伙伴资料、本地头像处理及 GitHub Pages 发布。
- 标签指向 v3.0 开始修改前的完整远端基线，并包含当时已有的项目说明文档。
